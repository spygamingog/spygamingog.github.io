# SpyGaming Website

This folder contains the source code for the **SpyGaming** website.

## 🚀 Setup Instructions for GitHub Pages

1.  **Initialize Git Repo**:
    Open a terminal in this `spyweb` folder and run:
    ```bash
    git init
    git add .
    git commit -m "Initial commit"
    ```

2.  **Connect to GitHub**:
    Replace `YOUR_USERNAME` with your GitHub username:
    ```bash
    git branch -M main
    git remote add origin https://github.com/spygamingog/spygamingog.github.io.git
    git push -u origin main
    ```

3.  **Configure GitHub Pages**:
    - Go to your repository settings on GitHub.
    - Navigate to **Pages** (in the sidebar).
    - Under **Build and deployment**, select **Source** as `Deploy from a branch`.
    - Select `main` branch and `/ (root)` folder.
    - Click **Save**.

## 🌐 Features
- Modern, responsive design.
- Click-to-copy server IP functionality.
- Showcase for SpyCore, SpyHunts, and SpyNetherPortals plugins.
- Integration with Discord and GitHub links.

## 🛠️ Customization
- Edit `index.html` to update text, links, and plugin descriptions.
- Edit `style.css` to change colors, fonts, and layout.
